# Documentation Sync Report

- Direction: full
- Mode: incremental, fixes enabled
- Audited at: 2026-09-02T09:59:11Z
- Base revision: `1a44cbffca1704bc87d24244ad07a00fe53091ae`
- Artifact-root fallback: `.planning/artifacts` (`aiwg` CLI and redirect settings were unavailable)

## Scope

Current DuckDB integration changes only:

- `pyproject.toml`
- `src/timesfm3/explorer.py`
- `src/timesfm3/run_store.py`
- `streamlit_app.py`
- `tests/test_explorer.py`
- `tests/test_run_store.py`
- `tests/test_streamlit_app.py`
- `README.md`
- DuckDB-related documents under `docs/`

Four pre-existing modified example media files were excluded.

## Findings

1. Code and documentation agree that CSV and Parquet uploads are read through DuckDB using temporary files that are deleted after parsing.
2. Code and documentation agree that decoded uploads remain in Streamlit session memory and raw uploads are not persisted.
3. Code and documentation agree that the newest 25 derived runs are persisted in `data/timesfm.duckdb`.
4. Code and documentation agree that restored runs omit input history and retain forecasts, metrics, settings, mappings, and manifests.
5. Code and documentation agree that persistence errors fall back to browser-session run history with a visible warning.
6. No stale claims about three-run retention, session-only run history, or absence of a database remain in the audited project documentation.

## Changes Applied by This Sync

None. The existing DuckDB documentation changes were already consistent with the implementation and tests.

## Human Review

- Future public or multi-user deployment still requires a decision on authentication, isolation, and whether local DuckDB should be replaced by a service datastore.

## Validation

- `uv run pytest -q tests/test_explorer.py tests/test_run_store.py tests/test_streamlit_app.py` — 41 passed
- `uv run ruff check src/timesfm3/explorer.py src/timesfm3/run_store.py streamlit_app.py tests/test_explorer.py tests/test_run_store.py tests/test_streamlit_app.py` — passed
- `uv run ty check src/timesfm3/explorer.py src/timesfm3/run_store.py streamlit_app.py` — passed
- `git diff --check -- . ':(exclude)timesfm-forecasting/examples/**'` — passed; Git emitted line-ending notices only

## Recommended Handoff

Use the repository's commit-and-push workflow when these existing DuckDB changes are ready to publish.
